name: Build ESP32 Marauder

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install PlatformIO
        run: pip install platformio

      - name: Install required Arduino libraries
        run: |
          platformio lib install "TFT_eSPI"
          platformio lib install "mathieucarbou/ESP Async WebServer@3.0.6"
          platformio lib install "bblanchon/ArduinoJson"

      - name: Build firmware
        run: platformio run

      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: esp32-marauder-firmware
          path: .pio/build/**/firmware.bin
